# LogistiQ

India-first supply-chain SaaS for e-commerce and quick-commerce sellers: 31 free calculators, an **AI Demand Planner**, an **FBA Planner**, Sourcing & Procurement, and a TMS. Static site + Vercel serverless functions.

This README documents the **God-mode engine upgrades** (v11) layered on top of the existing product. Every upgrade is **additive and backend-only** — it changes no page visuals and no written/marketing copy. New capabilities are exposed as new fields in the API responses and as new serverless engines; the existing UI keeps working unchanged and can surface the new numbers when wired.

---

## What's new (v11 — additive engine layer)

| Module | Powers | What it adds |
|---|---|---|
| `lib/econ.mjs` | AI Demand Planner | India unit economics — commission, fees, GST, and **RTO** per channel → **true rupee contribution** per SKU; profit-leak detection. |
| `lib/probabilistic.mjs` | AI Demand Planner, FBA | **P50/P90/P95** quantile forecasts, interval-coverage check, newsvendor reorder point. |
| `lib/buyplan.mjs` | AI Demand Planner | **Budget-constrained buy plan** (protected-contribution-per-rupee, with partial-fill) + supplier / dark-store **purchase orders**. |
| `api/fba-forecast.mjs` | **FBA Planner (transformed)** | Full FBA demand-planning engine: auto model + probabilistic band, **restock-limit-capped send plan**, FBA fee economics, **LTSF/aged-inventory risk + removal**, stranded detection, Q4 storage forecast, Gemini insights. |
| `lib/sourcing.mjs` | Sourcing & Procurement | Supplier scorecard (OTIF/lead-time/price/quality/fill), landed-cost comparison, reorder → PO (feeds inbound back to the planner). |
| `lib/tms-rateshop.mjs` | TMS | **RTO-aware carrier rate-shop** (weight-slab + zone + COD), cost-per-delivered-order feeding unit economics. |
| `lib/normalize.mjs` | Connectors | Canonical normalizer: Shopify + Amazon SP-API payloads → engine CSV. Add a marketplace = one normalizer. |
| `api/connect/shopify.mjs` | Connectors | **Shopify** OAuth install/callback + HMAC verify + Admin-API pull → normalize (secrets from env). |
| `api/connect/amazon.mjs` | Connectors | **Amazon SP-API** LWA token → Orders + FBA Inventory pull → normalize (official API, no scraping). |
| `lib/history.mjs` + `js/liq-history.js` | Saved history | Merge to lengthen the series + **forecast-vs-actual** accuracy badge, persisted in the seller's existing Firestore (no new secrets). |
| `lib/gstin.mjs` + `js/liq-paperwork.js` | Sourcing + Paperwork Hub | **GSTIN validation** (format + checksum) on any GST field, + a Gemini "Paperwork AI" panel (paste→line-items, HSN suggestions, EXIM Q&A). |
| `js/liq-sourcing.js` | Sourcing (Ecom + ERP) | **Supplier scorecard + landed-cost comparison** UI (mirrors `lib/sourcing.mjs`) with Gemini "explain / negotiation tips" buttons. |
| `api/ai.mjs` | All tools | **One shared Gemini endpoint** (task-based) for narrative/drafting/extraction. Hard guardrail: never asserts authoritative numbers/GSTIN/HSN/tax/legal — drafts, explains, extracts, suggests (verify). |

The AI Demand Planner engine (`api/forecast.js`) now returns, additively: per-SKU `econ` + `forecastQuantiles`, and top-level `buyPlan`, `purchaseOrders`, and `profitLeaks`.

See `LogistiQ-AI-Demand-Planner-God-Mode-Blueprint.md` for the full strategy and roadmap.

---

## Architecture notes

- **Module system is mixed, by design.** `forecast.js`, `copilot.mjs` and all new engines use **ESM**; `distance.js`, `sr-rates.js`, `sr-track.js`, `tms-ai.js`, `_sr.js` use **CommonJS**. There is intentionally **no root `package.json`** — Vercel detects module type per file. New engines ship as **`.mjs`** so they are unambiguously ESM everywhere (Vercel + local Node) without a `package.json`, and never affect the CommonJS functions.
- **Self-contained engines.** `fba-forecast.mjs`, `sourcing.mjs`, `tms-rateshop.mjs` import at most `probabilistic.mjs`, so they run in plain Node with zero setup. `forecast.js` imports `econ.mjs` / `probabilistic.mjs` / `buyplan.mjs`.
- **Privacy model preserved.** Gemini (`gemini-2.5-flash`) writes *narrative insights only*, from computed summary numbers — never raw uploaded data. Forecast figures are always computed, never generated by the LLM.
- **Vercel function budget.** Every file in `/api` becomes a serverless function, and the Hobby plan caps a deployment at **12**. So only HTTP **endpoints** live in `/api`; shared **libraries** live in `/lib` and are bundled into whichever functions import them (they don't count). Files prefixed `_` (e.g. `_sr.js`) are private helpers Vercel ignores. This keeps the deployment at 10 functions.

## Environment variables

Set in Vercel Project Settings → Environment Variables (never commit secrets):

```
GEMINI_API_KEY=<your key>        # powers /api/forecast, /api/fba-forecast AND /api/ai (sourcing + paperwork assist)

# Connectors (optional — only if you enable Shopify / Amazon sync). See CONNECTORS.md.
SHOPIFY_API_KEY=...
SHOPIFY_API_SECRET=...           # never commit
SHOPIFY_SCOPES=read_orders,read_products,read_inventory
APP_URL=https://YOUR_DOMAIN
LWA_CLIENT_ID=...
LWA_CLIENT_SECRET=...            # never commit
SPAPI_REFRESH_TOKEN=...          # never commit
SPAPI_HOST=sellingpartnerapi-eu.amazon.com
```

Both planners fall back to deterministic insights when `GEMINI_API_KEY` is absent, so they work without it. Connectors are optional; the planners work upload-only until you configure them. **Saved history/accuracy needs no secrets** — it reuses the seller's existing Firestore account.

## Fee / economics assumptions

All commission, fee, storage, RTO and carrier figures in `econ.mjs`, `fba-forecast.mjs` and `tms-rateshop.mjs` are **indicative India defaults** — directional planning inputs, seller-overridable, and superseded by real values when present in the uploaded reports. They are not billed rates. Always validate against your ERP/WMS/marketplace before purchasing.

---

## Running the tests

Plain Node ≥ 18, no dependencies, no build step:

```bash
node test/engine.test.mjs      # econ + probabilistic + buyplan   (49 assertions)
node test/fba.test.mjs         # FBA forecasting engine           (20 assertions)
node test/tools.test.mjs       # sourcing + TMS engines           (20 assertions)
node test/connectors.test.mjs  # normalizers + HMAC + history     (24 assertions)
node test/paperwork.test.mjs   # GSTIN validation + AI prompts    (14 assertions)
```

All five should print `passed, 0 failed` (127 assertions total).

## Deploy

Deploys as-is on Vercel (static files + `/api` serverless functions). `vercel.json` sets a 30s max duration for `api/forecast.js` and `api/fba-forecast.mjs`.

---

## Repo map (key paths)

```
api/   ← each file = ONE Vercel serverless function (Hobby plan caps at 12). Only ENDPOINTS live here.
  forecast.js         AI Demand Planner endpoint (+ v11 econ/quantiles/buyplan wiring)
  fba-forecast.mjs    FBA demand-planning endpoint (+ Gemini insights)
  connect/shopify.mjs Shopify OAuth + pull endpoint
  connect/amazon.mjs  Amazon SP-API pull endpoint
  ai.mjs              shared Gemini endpoint (sourcing/paperwork narrative, drafting, extraction)
  copilot.mjs, distance.js, sr-rates.js, sr-track.js, tms-ai.js   (existing endpoints)
  _sr.js              (existing private helper — underscore prefix → Vercel does NOT count it)
lib/   ← shared libraries: imported by the endpoints, bundled at build, NOT counted as functions
  econ.mjs            India unit economics
  probabilistic.mjs   quantile forecasts + newsvendor
  buyplan.mjs         budget-constrained buy plan + POs
  sourcing.mjs        supplier scorecard + landed cost + PO
  tms-rateshop.mjs    RTO-aware carrier rate-shop
  history.mjs         merge history + forecast-vs-actual (pure)
  normalize.mjs       Shopify/Amazon payload → engine CSV
  gstin.mjs           GSTIN format + checksum validation
js/
  liq-history.js      saved history + accuracy badge (reuses Firestore)
  liq-sourcing.js     supplier scorecard + landed-cost UI (+ Gemini)
  liq-paperwork.js    GSTIN validation + Paperwork AI panel (+ Gemini)
  liq-auth.js, liq-cloud.js, liq-icons.js, liq-logo.js   (existing)
test/
  engine.test.mjs, fba.test.mjs, tools.test.mjs, connectors.test.mjs, paperwork.test.mjs
CONNECTORS.md         connector setup guide (Shopify + SP-API + env vars)
demand-planner.html, fba-planner/, sourcing-procurement.html, tms.html, *-calculator.html, ...
```

Current `/api` function count: **10** (well under the Hobby-plan limit of 12).
